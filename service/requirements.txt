# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0

# Validation and settings
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.0.0

# HTTP client
httpx>=0.27.0
tenacity>=8.2.0

# Background tasks
celery>=5.3.0
redis>=5.0.0

# Templates and file handling
jinja2>=3.1.0
python-multipart>=0.0.6

# Authentication
passlib[bcrypt]>=1.7.0
argon2-cffi>=23.0.0
python-jose[cryptography]>=3.3.0

# PDF generation
playwright>=1.48.0
markdown>=3.5.0

# Logging and serialization
structlog>=24.1.0
orjson>=3.9.0

# Document processing
pymupdf>=1.24.8
python-docx>=1.1.0

# LLM providers
openai>=1.0.0
anthropic>=0.37.0
google-genai>=0.2.0  # Newer SDK with grounding/web search support (recommended)
google-generativeai>=0.1.0  # Older SDK (fallback for backward compatibility)

# Streaming and utilities
sse-starlette>=1.8.0
python-dotenv>=1.0.0
json-repair>=0.7.0

# Optional: S3 storage (can be removed if using local storage only)
boto3>=1.34.0
